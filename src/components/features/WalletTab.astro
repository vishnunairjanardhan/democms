<section class="flex items-end justify-center border-y border-white/5">
    <div class="px-8 mx-auto md:px-12 lg:px-16 max-w-7xl lg:py-24 ">
        <div class="text-center">
        <h2>Design Stunning Passes with Our Intuitive Builder.
            <br/>Three simple steps
        </h2>
        <p class="mt-5">Get your mobile wallet passes up and running in minutes, not days.
        </p>
        </div>
        <div class="flex items-start mt-20 -mb-px" x-data="{ openTab: 1 }">
            <div class="border-l border-vulcan-700 w-full">
                <div class="{ 'border-l-2 border-indigo-500 transition: border-color 1s ease-in-out': openTab === 1 }" class="relative flex flex-col w-full select-none lg:flex-col lg:inline-grid md:flex-row transition: all 1s ease-in-out">
                    <button 
                        @click="openTab === 1 ? openTab = null : openTab = 1" 
                        type="button" 
                        class="relative z-20  py-1 my-2 inline-flex items-center w-auto text-sm font-medium transition-all cursor-pointer px-3 text-white text-left" 
                        id="tabs-7-1">
                        <h3 class="ml-1"> Design your pass</h3>
                    </button>
             
                <div 
                x-show="openTab === 1" 
                x-transition:enter-start="opacity-0 transform -translate-y-2" 
                x-transition:enter-end="opacity-100 transform translate-y-0" 
                x-transition:enter="transition duration-500" 
                x-transition:leave="transition duration-500" 
                x-transition:leave-start="opacity-100 transform translate-y-0" 
                x-transition:leave-end="opacity-0 transform -translate-y-2"
                
            >
                    <div class="relative items-center w-full">
                        <div>
                            <div class="sm:order-1">
                                <p class="px-4">Choose a template and customize it with your branding, colors, and content using our intuitive visual editor. No coding required</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
     
        <div :class="{ 'border-l-2 border-indigo-500': openTab === 2 }" class="relative flex flex-col w-full select-none lg:flex-col lg:inline-grid md:flex-row">
        <button 
         @click="openTab === 2 ? openTab = null : openTab = 2" 
         type="button" 
         class="relative z-20  py-1 my-2 inline-flex items-center text-left w-auto text-sm font-medium transition-all cursor-pointer px-3 text-white" 
         id="tabs-7-2"
        >
       <h3 class="ml-1">Configure settings</h3>
    </button>

            <div 
            x-show="openTab === 2" 
 
            x-transition:enter-start="opacity-0 transform -translate-y-2" 
            x-transition:enter-end="opacity-100 transform translate-y-0" 
            x-transition:enter="transition duration-500" 
            x-transition:leave="transition duration-500" 
            x-transition:leave-start="opacity-100 transform translate-y-0" 
            x-transition:leave-end="opacity-0 transform -translate-y-2"
    
             >
                           <div class="relative items-center w-full">
                           <div>
                            <div class="sm:order-1">
                               <p class="px-4">Set up pass behavior, expiration dates, locations, and notifications to create a dynamic experience.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
       
      <div :class="{ 'border-l-2 border-indigo-500': openTab === 3 }" class="relative flex flex-col w-full   select-none lg:flex-col lg:inline-grid md:flex-row">
        <button 
        @click="openTab === 3 ? openTab = null : openTab = 3" 
        type="button" 
        class="relative z-20  py-1 my-2 inline-flex items-center text-left w-auto text-sm font-medium transition-all cursor-pointer px-3 text-white"
        id="tabs-7-3"
       >
      <h3 class="ml-1"> Distribute & analyze</h3>
    </button>
 
              <div 
              x-show="openTab === 3" 
   
              x-transition:enter-start="opacity-0 transform -translate-y-2" 
              x-transition:enter-end="opacity-100 transform translate-y-0" 
              x-transition:enter="transition duration-500" 
              x-transition:leave="transition duration-500" 
              x-transition:leave-start="opacity-100 transform translate-y-0" 
              x-transition:leave-end="opacity-0 transform -translate-y-2"
    
              >
                    <div class="relative items-center w-full">
                        <div>
                            <div class="sm:order-1">
                               <p class="px-4">Share passes via email, SMS, QR codes, or direct links, then track performance with detailed analytics.</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
             <!-- Image starts here -->
            <div class='w-3/4 hidden md:inline'> 
                <div 
                x-show="openTab === 1" 
     
                x-transition:enter-start="opacity-0 transform -translate-y-2" 
                x-transition:enter-end="opacity-100 transform translate-y-0" 
                x-transition:leave-start="opacity-100 transform translate-y-0" 
                x-transition:leave-end="opacity-0 transform -translate-y-2"
      
                 >
                <div class="relative items-center w-full">
                    <div>
                        <div class="sm:order-2">
                            <img alt="Bulk Gift Card" loading="lazy" class="rounded-2xl  shadow-vulcan-950/50 w-full" src="assets/wallet/wallet_config.webp">
                        </div>
                    </div>
                </div>
            </div>

            <div 
            x-show="openTab === 2" 
 
            x-transition:enter-start="opacity-0 transform -translate-y-2" 
            x-transition:enter-end="opacity-100 transform translate-y-0" 
            x-transition:leave-start="opacity-100 transform translate-y-0" 
            x-transition:leave-end="opacity-0 transform -translate-y-2"
  
             >
                <div class="relative items-center w-full">
                <div>
                 <div class="sm:order-2">
                   <img alt="Lexington thumbnail" loading="lazy" class="rounded-2xl shadow-vulcan-950/50 w-full" src="/assets/giftcard/import-migration1.webp">
                 </div>
                </div>
             </div>
         </div>
     
         <div 
         x-show="openTab === 3" 
         x-transition:enter-start="opacity-0 transform -translate-y-2" 
         x-transition:enter-end="opacity-100 transform translate-y-0" 
         x-transition:leave-start="opacity-100 transform translate-y-0" 
         x-transition:leave-end="opacity-0 transform -translate-y-2"

          >
        <div class="relative items-center w-full">
            <div>
                <div class="sm:order-2">
                 <img alt="Lexington thumbnail" loading="lazy" class="rounded-2xl shadow-vulcan-950/50 w-full" src="/assets/wallet/Wallet-banner.png">
                </div>
            </div>
        </div>
    </div>
    </div>
  </div>
</div>
</section>

  

   